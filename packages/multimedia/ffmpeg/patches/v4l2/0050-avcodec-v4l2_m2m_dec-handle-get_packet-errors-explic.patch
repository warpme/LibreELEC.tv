From 46e09c3b1217e52104512072c641e9ff0a47282d Mon Sep 17 00:00:00 2001
From: Aman Gupta <aman@tmm1.net>
Date: Thu, 12 Sep 2019 10:27:22 -0700
Subject: [PATCH 50/59] avcodec/v4l2_m2m_dec: handle get_packet errors
 explicitly

Signed-off-by: Aman Gupta <aman@tmm1.net>
---
 libavcodec/v4l2_m2m_dec.c | 12 ++++++++----
 1 file changed, 8 insertions(+), 4 deletions(-)

diff --git a/libavcodec/v4l2_m2m_dec.c b/libavcodec/v4l2_m2m_dec.c
index bc0b71d262..1657f6d7de 100644
--- a/libavcodec/v4l2_m2m_dec.c
+++ b/libavcodec/v4l2_m2m_dec.c
@@ -133,15 +133,19 @@ static int v4l2_receive_frame(AVCodecContext *avctx, AVFrame *frame)
     V4L2Context *const output = &s->output;
     int ret;
 
+    if (s->draining)
+        goto dequeue;
+
     if (!s->buf_pkt.size) {
         ret = ff_decode_get_packet(avctx, &s->buf_pkt);
-        if (ret < 0 && ret != AVERROR_EOF)
+        if (ret == AVERROR(EAGAIN))
+            goto dequeue;
+        else if (ret == AVERROR_EOF)
+            return ff_v4l2_context_enqueue_packet(output, &s->buf_pkt); /* null pkt */
+        else if (ret < 0)
             return ret;
     }
 
-    if (s->draining)
-        goto dequeue;
-
     ret = ff_v4l2_context_enqueue_packet(output, &s->buf_pkt);
     if (ret == AVERROR(EAGAIN)) {
         /* no input buffers available, continue dequeing */
-- 
2.21.0

